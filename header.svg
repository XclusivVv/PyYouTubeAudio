<svg width="100%" height="100" viewBox="0 0 600 100" xmlns="http://www.w3.org/2000/svg">
  <style>
    .title {
      font-size: 40px;
      fill: white;
      animation: pulse 2s infinite alternate;
    }
    @keyframes pulse {
      0% { fill: #00ffff; }
      100% { fill: #ff00ff; }
    }
    .bg {
      animation: bgshift 10s infinite alternate;
    }
    @keyframes bgshift {
      0% { fill: #111122; }
      100% { fill: #222211; }
    }
  </style>
  <rect class="bg" width="600" height="100" />
  <text x="50%" y="60" text-anchor="middle" class="title">🎧 PyYouTubeAudio</text>
</svg>